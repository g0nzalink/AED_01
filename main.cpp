#include <iostream>
/*
int main() {
    char *ptr; char Str[] = "abcdefg";
    ptr = Str;
    ptr += 5;
    std::cout << ptr;
    return 0;
}

Hay un puntero que apunta al inicio de un array de char, de ahi se mueve 5 posiciones desde el inicio del array
llegando a f, y cuando se imprime el puntero se imprime el array desde la posicion que esta apuntando



int main() {
    int arr[3] = {1, 2, 3}; //crea el array 1 2 3
    int *ptr = arr; //apunta al inicio del array
    *(ptr + 1) = *(ptr + 1) + 5; //cambia el valor del puntero pero un paso mas, es decir, al 2 le suma 5
    ptr++; //ahora punta a la segunda posicion, es decir ahora apunta a 2
    *ptr = *(ptr - 1) + 5; //el 2 ahora es igual a una posicion menos del puntero (el 1) mas 5, es decir ahora es 6
    std::cout << arr[0] << " " << arr[1] << " " << arr[2]; //imprime todo
    return 0;
}

*/

